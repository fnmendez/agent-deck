# Diamond review — conductor overlay (2026-08-26 01:30–01:50, Mac)

Artifact frozen (SHA256): bridge_local.py 394f3442…, reapply.sh 3538b816…, bridge-hook.diff babd1b96….
Lanes (independent, no shared conclusions): A = GPT-5.6 Sol xhigh via mcodex (read-only), correctness lens;
B = GPT-5.6 Sol xhigh via mcodex (read-only), security lens; C = Claude Fable 5 subagent. All completed.
Raw findings: diamond-2026-08-26-lane{A,B,C}-*.md. Adjudicator: conductor-cmds (Claude), by evidence.

## Accepted (applied, re-tested: 34 unit tests, reapply x2, Telegram e2e /agents /peek /send)
- A1/B1/C1 HIGH: Telegram text could be parsed as CLI flags (`--message-file` = arbitrary file read into a pane). Fix: flags first, then `--`, id, message (verified live: `--message-file /etc/hosts …` arrived as literal text).
- A5/B4/C7 HIGH: bridge.py truncated before validation. Fix: tmp + py_compile + os.replace.
- A2/B2 HIGH: 8-char prefix ownership too loose. Fix: exact match or ≥60-char prefix only.
- A11/B10/C6 MED: restart verify false positives (`Connection established` only logged after a failed poll; OR-condition; second-granularity). Fix: byte-offset of log, require `overlay: registered` (or `Run polling for bot` on rollback), pid stable 3 s, no overlay error/Traceback.
- A10/B8 MED: foreign composer text echoed to Telegram. Fix: report length only.
- A7 MED: unverified delivery shown as ✅. Fix: ⚠️ wording.
- A8/B6 MED: rescue errors escape / rc ignored. Fix: try/except + rc checks on tmux and captures.
- A4/B5 MED: overlay handler exception leaves command unanswered. Fix: `guarded` wrapper (log + reply); register handlers all-or-nothing.
- A6/A12/B9 MED: marker/anchor not fail-closed, module never validated. Fix: exact counts, preflight compile+import, sha-tracked restart.
- B11 MED: settings.json rewrite non-atomic. Fix: copy backup, tmp+fsync+replace.
- A13/B7 MED: titles with spaces. Fix: quoted title support.
- A14 MED: help not stock+additions. Fix: conductors, /sessions alias, routing note added.
- A15/B12 LOW: hardcoded ~/.config. Fix: use bridge's `resolve_config_path` via ctx.
- A9 MED: /peek fails open on unknown UI. Fix: refuse when no ❯/› prompt found.
- A16, C2, C3, C4, C5, C8, C9, B13 LOW: peek budget incl. header; html.escape title; DIM_RE closes on any SGR and accepts `2;..m`; collapsed-paste wording; /peek usage text; JSON failure to stderr; rollback refuses without hook; missing plist/venv fail loudly.

## Rejected / deferred (with reason)
- A3/B3 HIGH "remove the rescue Enter (race not closable)": the guarded rescue is a locked design decision (ops-main approval 01:2x); residual window mitigated by immediate re-capture; documented in README as accepted residual risk. Franco may revisit.
- A9 alt "restrict /peek to explicit tool list": superseded by fail-closed prompt detection.
- Lanes' notes on "Claude/CodeRabbit second opinion unavailable" are about their own sandbox; lane C supplied the second model family.
